schema_version: 1

context:
  name: pulsar-client
  version: "3.12.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://github.com/apache/pulsar-client-python/archive/refs/tags/v${{ version }}.tar.gz
  sha256: 6e1d8da6dad81e4185cf1ae2c65bcea523065ab7fa998bd8eabaf13f611da1c6

build:
  number: 0

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ stdlib("c") }}
    - ${{ compiler('cxx') }}
    - cmake
    - ninja
    - if: not win
      then: sed
      else: m2-sed
    - if: build_platform != target_platform
      then:
        - cross-python_${{ target_platform }}
        - python
  host:
    - python
    - pip
    - setuptools
    - libpulsar
    - pybind11
  run:
    - python
    - certifi

tests:
  - python:
      imports:
        - pulsar

about:
  license: Apache-2.0
  license_file: LICENSE
  summary: Apache Pulsar Python client library
  homepage: https://github.com/apache/pulsar-client-python

extra:
  recipe-maintainers:
    - xhochy
